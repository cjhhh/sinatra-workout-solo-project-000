<% if flash.has?(:message) %>
<%= flash[:message] %>
<% end %>

<h1> Welcome, <%= current_user.username.capitalize %>!</h1>
<p> Here you can display your workout: </p>

 <form method="POST" action='/workouts'>
  <label> What shall you name your custom workout? </label>
  <input type="text" name="user[workout][title]" placeholder="Title:"><br></br>

  <label> Describe the workout in full! We're talking about reps, sets, names etc. </label>
  <textarea type="text" name="user[workout][content]" placeholder="Content:"></textarea><br></br>

  <label>What primary body part will this workout?</label>
  <% Part.all.each do |p| %>
  <input type="radio" name="user[workout][parts][name]" id="<%= p.id %>" value="<%= p.name %>"><%= p.name %></input><br></br>
  <% end %>

  <input type="submit" value="Submit Workout">
</form>
